#!/usr/bin/env bash

set -euxo pipefail

systemctl stop lord

for host in alpha bravo; do
  rsync \
    --archive \
    --progress \
    /var/lib/lord/index.redb \
    root@$host.ordinals.net:/var/lib/lord/index.redb.new
done

systemctl restart lord